GET METHOD
- GET METHOD 는 리소스를 취득할 때 사용을 하고 PathVariable 을 가질 수 있다.

PATH Variable
- 주소 내에 정보를 전달하는 방법
- 그래서 GET METHOD 에서 특정한 리소스의 정보를 가져올 때 사용
- https://www.foo.bar/user-id/100 -> USER ID 가 100 인 사용자의 정보를 요청할 때 ( 100 번이라는 부분이 유동적으로 변할 수 있음 )
- https://www.foo.bar/user-id/100/card-id/200
- 주소에 값을 넣어준다
- 단점 : 주소가 노출되기 때문에 얼마든지 이 주소에 대한 값을 변환시켜 볼 수 있다 그래서 서버에서는 항상 보안에 대해 대비해야 됨

한글 글자 깨짐
- 윈도우는 기본적으로 ms949 라는 인코딩을 사용한다.
- 그렇기 때문에 우리가 UTF-8 로 요청을 하게 되면 인코딩이 일치하지 않기 때문에 글자가 깨지는 현상이 일어남
- 웹 서버는 UTF-8 사용 윈도우는 MS949 사용

[ 이미 사용중인 포트 종료시키는 방법 ]
8080 포틀를 사용중인 프로세스 보는 명령어
- netstat -ano | findstr 8080

27852 프로세스 죽이는 명령어
- taskkill /f /pid 27852